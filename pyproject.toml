[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rpg"
version = "0.1.0"
description = "TDD Python RPG with clean architecture"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.black]
line-length = 88
